function setCenter(_state) {
  if (_state === true) {
      ctx.transform(1,0,0,1,100,100);
  } if (_state === false) {
      ctx.transform(1,0,0,1,0,0);
  }
}

function onstop() {
  ctx.transform(1,0,0,1,0,0);
}
